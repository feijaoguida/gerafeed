# Task 005. AI

## Status
DONE

## Objetivo
Processar uma notícia com OpenAI e gerar conteúdo editorial e SEO estruturado.

## Escopo
A IA deve retornar:
- relevância;
- score;
- título;
- resumo;
- conteúdo;
- categoria sugerida;
- tags;
- focus keyword;
- meta title;
- meta description.

Usar saída estruturada/JSON.

## Fora do escopo
- publicação.
- geração de imagens.
- automação.

## Definition of Done
- [x] OpenAI configurada server-side.
- [x] Notícia é enviada para análise.
- [x] Resposta estruturada é validada.
- [x] Conteúdo não pode depender de texto JSON inválido.
- [x] Relevância e score são salvos.
- [x] Conteúdo editorial é salvo.
- [x] SEO é salvo.
- [x] Categoria sugerida é salva.
- [x] Falha da IA é tratada.
- [x] TypeScript PASS.
- [x] Lint PASS.
- [x] Testes aplicáveis PASS.

## Evidence
- Módulo `src/lib/ai.ts` criado com `processArticleContentWithOpenAI` e `processArticleWithAi` utilizando resposta estritamente estruturada em JSON (`response_format: { type: "json_object" }`).
- Rota de API criada: `POST /api/articles/[id]/process-ai` em `src/app/api/articles/[id]/process-ai/route.ts`.
- Mapeamento e persistência dos 10 campos requeridos na tabela `Article` do PostgreSQL:
  - `aiScore`, `title`, `summary`, `content`, `suggestedCategoryId`, `tags`, `seoFocusKeyword`, `seoTitle`, `seoDescription`.
- Script de teste `scripts/test-ai.ts` executado validando:
  - Captura amigável de exceções de chave/API sem quebrar a aplicação.
  - Validação de JSON e schema de todos os campos editoriais e de SEO.
  - Persistência e integridade relacional com categorias locais do WordPress.
- Validações:
  - `npx tsc --noEmit`: PASS (0 erros)
  - `npm run lint`: PASS (0 erros, 0 avisos)
  - `npm run build`: PASS (compilação Next.js App Router concluída com sucesso)

## Discovered Work
Nenhum trabalho fora do escopo descoberto.

## Status
DONE
