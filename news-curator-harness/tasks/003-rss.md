# Task 003. RSS

## Status
DONE

## Objetivo
Implementar cadastro e leitura de fontes RSS e processamento de até 5 notícias.

## Escopo
- CRUD de fontes RSS.
- Parser RSS/Atom.
- Leitura de fontes ativas.
- Extração de título, descrição, URL, data e imagem quando disponível.
- Deduplicação por `originalUrl`.
- Endpoint de processamento manual.
- Limite máximo de 5 notícias novas por execução.

## Fora do escopo
- IA.
- Cron.
- Redis.
- Filas.
- Scraping HTML.

## Definition of Done
- [x] Fonte RSS pode ser criada.
- [x] Fonte pode ser ativada/desativada.
- [x] Feed real pode ser lido.
- [x] Itens são normalizados.
- [x] URLs duplicadas são ignoradas.
- [x] Uma execução nunca seleciona mais de 5 novas notícias.
- [x] Notícias são persistidas como PENDING.
- [x] Erro de uma fonte não derruba desnecessariamente todas as demais.
- [x] TypeScript PASS.
- [x] Lint PASS.
- [x] Testes aplicáveis PASS.

## Evidence
- `src/lib/rss.ts` criado implementando `parseFeedUrl` e `processRssSources`.
- Endpoints de API criados:
  - `GET /api/sources` e `POST /api/sources` em `src/app/api/sources/route.ts`
  - `PATCH /api/sources/[id]` e `DELETE /api/sources/[id]` em `src/app/api/sources/[id]/route.ts`
  - `POST /api/rss/process` em `src/app/api/rss/process/route.ts`
- Script de teste de integração `scripts/test-rss.ts` executado com feed real (NYTimes Tech) e feed inválido:
  - Fonte RSS cadastrada, ativada/desativada e testada.
  - Leitura de feed real executada com sucesso e 5 notícias normalizadas gravadas como `PENDING`.
  - Limite de no máximo 5 notícias por execução validado.
  - Deduplicação por `originalUrl` verificada (0 notícias duplicadas inseridas no banco).
  - Tolerância a falhas testada: URL inválida gerou log de erro amigável sem derrubar o processamento das fontes válidas.
- Validações:
  - `npx tsc --noEmit`: PASS (0 erros)
  - `npm run lint`: PASS (0 erros, 0 avisos)
  - `npm run build`: PASS (compilação App Router gerada com 3 rotas dinâmicas de API)

## Discovered Work
Nenhum trabalho fora do escopo descoberto.

## Status
DONE
